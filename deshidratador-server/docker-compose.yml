# version: '3.8'
services:
  database:
    image: mysql:9.7
    container_name: mysql-db
    env_file: .env
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      DB_HOST: ${DB_HOST}
    ports:
      - "${DB_PORT}:3306"
    volumes:
      - database:/var/lib/mysql

  # node-app:
  #   # build: .
  #   restart: always
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   env_file: .env
  #   container_name: node-server
  #   environment:
  #     - MYSQL_DATABASE=${MYSQL_DATABASE}dc
  #     - MYSQL_USER=${MYSQL_USER}
  #     - MYSQL_PASSWORD=${MYSQL_PASSWORD}
  #     - DB_PASSWORD=${DB_PASSWORD}
  #     - DB_HOST=${DB_HOST}
  #     - DB_PORT=${DB_PORT}
  #   depends_on:
  #     - database
  #   ports:
  #     - "${NODE_PORT}:${NODE_PORT}"
  #   volumes:
  #     - .:/app
  #     - node_modules:/app/node_modules

volumes:
  # node_modules:
  database:
